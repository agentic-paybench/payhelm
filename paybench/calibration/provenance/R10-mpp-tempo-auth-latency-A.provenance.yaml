# PayBench calibration provenance — R10 MPP-Tempo (Session verify) — RAPL Sub-ranking A (Payment-Validation)
# Dimension 2 (authorization latency, RAPL). CALIBRATED from the first-party pilot
# {median, sigma} — the Variant-E mock baseline (methodology/dim2-auth-latency.md §5).
# Representative topology: local-complete; host-dependent (T2b median). The real multi-topology order + decomposition
# tuples are in methodology/dim2-scored-results.md (the single source of truth);
# this single-{median,sigma} fixture is the deterministic reproducibility baseline.

rail: "R10 MPP-Tempo (Session verify)"
dimension: authorization-latency:payment-validation
sub_ranking: "A"
unit: seconds                    # wall-clock, pay-command on the wire -> authorization-primitive decision

distribution:
  family: lognormal
  parameters: {median_s: 0.0082, sigma_log: 0.053}   # CALIBRATED from pilot/scored (T2b/canonical)
  approach: calibrated

sources:
  - type: first-party-pilot
    ref: "warm voucher-verify sigma (dim2-measurement-review-2) + scored T2b median (Group A1)"
    date: "2026-06-28"
    note: >
      Calibrated from the first-party RAPL pilot. local-complete; host-dependent (T2b median). See
      methodology/dim2-scored-results.md and dim2-q4-pilot-log.md.

calibration_date: "2026-06-29"
confidence: medium               # placeholder | low | medium | high
fixture_content_hash: "sha256:b3ae57ca385bb5d94a729f95ee9baa8456e43bec9f3056f328a3d4c9ddd9043f"
notes: >
  Variant-E calibrated mock baseline. Single {median,sigma} per (rail,sub-ranking)
  at the representative topology; not a published real-rail ranking. The scored
  within-group order is the claim (dim2-scored-results.md).
