# PayBench calibration provenance — R1 x402-Base AUTHORIZATION LATENCY
# Dimension 2 (Day-30, §11). PLACEHOLDER — pending founder calibration.
#
# !!! PLACEHOLDER — pending founder calibration !!!
# The {median_s, sigma_log} below are illustrative placeholders chosen only to
# exercise the harness end-to-end. They are NOT measured and MUST NOT be
# published or pre-registered. Real authorization-latency calibration is a
# founder step (first-party measurement of the 402-challenge / mandate-verify
# leg, doc/telemetry for spread). See methodology/dim2-auth-latency.DRAFT.md.

rail: "R1 x402-Base"
dimension: authorization-latency
unit: seconds                    # wall-clock, payment-intent presented -> authorization granted

distribution:
  family: lognormal
  parameters: {median_s: 0.45, sigma_log: 0.30}   # PLACEHOLDER — NOT measured
  approach: placeholder

sources:
  - type: placeholder
    ref: "PLACEHOLDER — no measurement; harness-exercising value only"
    date: "2026-06-15"
    sample_size: 0
    note: >
      Authorization latency for x402-Base = the HTTP 402 challenge -> signed
      payment-payload -> facilitator verify/accept round-trip, i.e. the time to a
      go/authorized signal BEFORE on-chain settlement (which is the R1 finality
      dimension). A first-party measure would instrument the x402 verify leg
      separately from settle. Placeholder median 0.45s / sigma_log 0.30.

calibration_date: "2026-06-15"
confidence: placeholder          # placeholder | low | medium | high
fixture_content_hash: "sha256:4e21222e0bc0c1c2d039f8c70570ec05afc9b68eaca07822db0db0c0806b56ef"
notes: >
  PLACEHOLDER — pending founder calibration. Do not publish or pre-register.
