# PayBench calibration provenance — R11 MPP-on-Spark-Lightning AUTHORIZATION LATENCY
# Dimension 2 (Day-30, §11). PLACEHOLDER — pending founder calibration.
#
# !!! PLACEHOLDER — pending founder calibration !!!
# The {median_s, sigma_log} below are illustrative placeholders chosen only to
# exercise the harness end-to-end. They are NOT measured and MUST NOT be
# published or pre-registered. See methodology/dim2-auth-latency.DRAFT.md.

rail: "R11 MPP-on-Spark-Lightning"
dimension: authorization-latency
unit: seconds                    # wall-clock, payment-intent presented -> authorization granted

distribution:
  family: lognormal
  parameters: {median_s: 0.70, sigma_log: 0.32}   # PLACEHOLDER — NOT measured
  approach: placeholder

sources:
  - type: placeholder
    ref: "PLACEHOLDER — no measurement; harness-exercising value only"
    date: "2026-06-15"
    sample_size: 0
    note: >
      Authorization latency for MPP-on-Spark-Lightning = the L402-family challenge
      -> BOLT11 invoice issuance + authorization, ahead of the Spark FROST+SSP
      preimage-release settlement (the R11 finality dimension, ~16.5s, dominated by
      the operator ceremony). Authorization (invoice issuance / 402 handshake) is
      far faster than that ceremony; a first-party measure should isolate the
      authorize leg. Placeholder median 0.70s / sigma_log 0.32.

calibration_date: "2026-06-15"
confidence: placeholder
fixture_content_hash: "sha256:fa2f50e2945bd678c415fcb7ced8c57aee903a14389a8681484d177536cb056f"
notes: >
  PLACEHOLDER — pending founder calibration. Do not publish or pre-register.
